import { ForgotPasswordScreen } from "@/screens/ForgotPassword";

/** Reset a forgotten password. Reached from the sign-in tab. */
export default function ForgotPassword() {
	return <ForgotPasswordScreen />;
}
